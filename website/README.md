# Website

This folder contains a static website that can be deployed to GitHub Pages.

## Local preview

Serve files with any static server from the repository root, for example:

```bash
python3 -m http.server 8080
```

Then open:

- `http://localhost:8080/website/index.html`
- `http://localhost:8080/website/docs.html`
